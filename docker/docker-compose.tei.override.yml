# Auto-generated by fix-tei-embed-throttle.sh — do not edit by hand.
# Recreate TEI: docker compose -f docker-compose.tei.yml -f docker-compose.tei.override.yml up -d --force-recreate
services:
  tei-embed:
    environment:
      - RAYON_NUM_THREADS=2
    command:
      - --model-id
      - intfloat/multilingual-e5-small
      - --port
      - "80"
      - --auto-truncate
      - --max-concurrent-requests
      - "3"
    cpus: 2.5
    mem_limit: 1024m
    pids_limit: 64
